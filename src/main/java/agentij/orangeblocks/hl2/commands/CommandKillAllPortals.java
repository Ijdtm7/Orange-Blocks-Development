package agentij.orangeblocks.hl2.commands;

import agentij.orangeblocks.hl2.common.tileentity.TileEntityPortal;
import net.minecraft.command.CommandBase;
import net.minecraft.command.CommandException;
import net.minecraft.command.ICommandSender;
import net.minecraft.command.WrongUsageException;
import net.minecraft.server.MinecraftServer;
import net.minecraft.world.World;
//TODO: Make this
public class CommandKillAllPortals {
    /*@Override
    public String getName() {
        return "killallportals";
    }

    @Override
    public String getUsage(ICommandSender sender) {
        return "command.killallportal.usage";
    }

    @Override
    public void execute(MinecraftServer server, ICommandSender sender, String[] args) throws CommandException {
        if (args.length > 0)
        {
            throw new WrongUsageException("command.killallportal.usage", new Object[0]);
        }else{
            World world = sender.getEntityWorld();

        }
    }*/
}
